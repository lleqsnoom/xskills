{
  "name": "x-skills-report-app",
  "private": true,
  "type": "module",
  "description": "Local app over the x-skills daily JSON packs. Not published: the npm package ships only bin, lib, skills and docs.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "build:panel": "vite build --mode panel"
  },
  "dependencies": {
    "solid-js": "^1.9.15"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^7.0.0",
    "vite-plugin-solid": "^2.11.0"
  }
}
