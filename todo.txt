Each task sould have its own file (one file per user story, one task per file)

Each file contains exactly one task with its goal, epic reference, files, effort, DOD, test plan, and dependencies.
Consolidated file removed.
No, tasks are not enough - they should be clear by itself without crossrerences

You're right — tasks should be fully self-contained: no file path cross-references, no "T2" dependency shortcuts, no
"handled by T2" in test plans. I'll also merge T1+T2 (both US1) into one task per user story, giving 7 files total. Each
will inline its own config fields, formulas, input mappings, and preconditions as descriptive text rather than
references.

If i open any task it should be clear for a developer what should be done/accomplished. Only task no crossreferenfce.
